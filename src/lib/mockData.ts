import type { SourceProfile } from '../types'

export const mockSourceProfile: SourceProfile = {
  topics: ['Linear equations', 'Word problems'],
  concepts: ['isolating variables', 'multi-step equations'],
  styleNotes: 'Homework uses step-by-step algebra with integer coefficients.',
}
